const Footer: React.FC = () => {
    return (
        <footer className="flex items-center justify-between py-2">
            <div>me@peterhogler.dev</div>
            <div>React, NextJS, TypeScript, Redux, Tailwind</div>
        </footer>
    );
};

export default Footer;
